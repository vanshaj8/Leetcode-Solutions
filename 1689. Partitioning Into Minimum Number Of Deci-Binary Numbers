class Solution {
public:
    int minPartitions(string n) 
    {
        int mini=INT_MIN;
        for(int i=0;i<n.length();i++)
        {
            int temp=n[i]-'0';
            
            mini=max(temp,mini);
        }
        return mini;
    }
};
