class Solution {
public:
    int totalMoney(int n) 
    {
	int weeks = n/7;
        
        int days = n%7;  
        
        return 28 * weeks + (weeks*(weeks-1))/2*7 + days*(days+1)/2 + days* weeks;

        
    }
};
